export interface User {
    id: string;
    username: string;
    user: string;
}
// filepath: /my-next-app/my-next-app/src/types/login.ts  
export interface LoginCredentials {
    user: string;
    password: string;
}